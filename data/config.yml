openai:
  model_name: gpt-4o-mini
  embedding_model: text-embedding-3-small
chunking:
  max_length: 750
  overlap_length: 150
retriever:
  top_k: 3
database:
  host: localhost
  port: 3306
  user: root
  password:
  database: generate_questions
logging:
  level: INFO
